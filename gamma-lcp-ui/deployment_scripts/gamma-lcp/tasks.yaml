- id: percona
  type: puppet
  groups: [primary-dbng, dbng]
  required_for: [deploy_end]
  requires: [att_cluster]
  parameters:
    puppet_manifest: /etc/puppet/modules/osnailyfacter/modular/gamma-lcp/percona.pp
    puppet_modules: /etc/puppet/modules
    timeout: 3600
